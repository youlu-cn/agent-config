# 仓库开发规范

本文件面向 AI 与开发者，是本仓库唯一的开发文档入口。用户说明放在各级 README；`rules/AGENTS.md` 是安装器分发的全局规则配置，不是本仓库开发文档，不要合并到这里。

## 1. 整体设计与职责

本仓库只管理可复用的全局规则和 harness 用户配置，不管理 harness 本体、凭据或运行状态。

- `rules/AGENTS.md`：跨 harness 的全局规则源。
- `harnesses/<name>/`：各 harness 独立配置及本地扩展。
- `install-rules.sh`、`install-harness.sh`：选择安装组、声明托管清单和必要迁移。
- `lib/install-managed.sh`：共用的差异检查、确认、备份、复制与回滚。
- `tests/` 与扩展目录中的测试：安装行为和扩展逻辑的回归验证。

Pi 源目录按职责组织，不镜像运行目录：

```text
harnesses/pi/
├── builtins/                  # 内置插件：一个插件一个目录
│   ├── fast/
│   │   ├── index.ts           # 插件入口
│   │   └── ...                # 实现、测试、用户说明
│   └── session-ui/
│       ├── index.ts
│       └── ...
├── plugin-configs/            # 插件配置：一个插件一个目录
│   ├── fast/config.json
│   ├── session-ui/config.json
│   └── ...                    # 其他插件配置及 Profile
└── config/                    # Pi 自身的 settings.json、keybindings.json
```

分层规则：

- `builtins/<插件名>/` 收拢该插件的全部源码、测试与用户说明，以 `index.ts` 为唯一入口；不得在 `builtins/` 根目录散放入口，也不得依赖兄弟插件的实现。
- `plugin-configs/<插件名>/` 保存可部署的插件配置和 Profile；插件源码中的配置解析器、类型与缺省行为仍属于源码，不与配置文件混淆。
- `config/` 只保存 Pi 本身的设置与快捷键，不混入插件源码或插件专属配置。
- 仓库布局表达职责，不复制用户 HOME 的路径结构；安装器负责映射运行路径，必要时在临时目录组合源码与配置。移动仓库文件不应强迫运行时适配仓库层级。

不收录 API Key、Token、私钥、登录态、sessions、cache、索引数据库或运行时包目录。配置默认值以配置文件为准，不在开发文档复制完整清单。

## 2. 安装器规范

- 安装真实文件，不改为软链接；一次只处理选中的配置组。全局规则的五个目标必须整组处理。
- 有冲突时先确认，覆盖前备份整组原配置；托管文件部署失败应回滚。拒绝安装不得继续执行清理或卸载。
- 目录配置、迁移与卸载都必须限定在安装目标内，不得误操作开发者真实 HOME 或其他 Pi agent 目录。
- 历史文件迁移须保留可恢复备份；外部包卸载须备份包和 npm 清单、禁用生命周期脚本，并复核实际残留，不能只依赖退出码。
- 外部卸载失败时保留新配置与备份，明确报告可重试状态，不宣称 npm 操作已回滚。
- 增删托管配置时同步检查安装清单和安装测试；复制子代理 Profile 不等于激活，不自动改写角色映射。
- session-ui 源码与配置先在临时目录组合，再作为一个完整目录参与差异检查、备份和替换；不把父目录和内部配置同时声明为独立托管项，避免重复备份与回滚冲突。
- 清理仅限明确托管的插件目录及已声明的旧文件，不清空整个 extensions 目录。保留用户额外插件、凭据和会话；安装失败须恢复组合目录的代码与配置。
- 改变插件入口时，旧入口以 `absent` 托管项参与同一次确认、备份和回滚；先备份旧入口再部署新目录，成功后只保留新入口，避免重复加载。拒绝安装不移除旧入口，安装失败须恢复旧入口与旧目录。

## 3. Pi 扩展设计

### session-ui：个人展示层

- 面向个人当前实际使用的插件，不为未启用插件、废弃实现或假想用户增加兼容分支。新增适配前核实实际接口，不提前引入通用发现、多版本协议或配置框架。
- `builtins/session-ui/index.ts` 是唯一入口，负责装配、配置和生命周期；适配逻辑将外部契约转为展示模型；布局与渲染消费展示模型。分层不要求每层独立建文件。
- 外部插件负责业务、请求策略、凭据和权威状态；session-ui 不复制其业务实现，不为展示重复获取数据，不将推测显示为已确认结果。
- 工具活动与工作动画只订阅生命周期，不重新注册工具、不替换正式 transcript renderer。标题由共享控制器统一写入，避免动画与 UI Meta 相互覆盖。
- 图片粘贴增强只改展示，保留 Pi 原生注册表、文本粘贴和提交展开行为；异步读取图片并使用有界缓存。依赖内部编辑器结构时使用运行时守卫，失配则警告并退回原生行为。
- 用量汇总只读会话条目，按原生 footer 口径累计带用量的记录，使用记录费用而非自行定价；不写会话、不触发预热、不读凭据、不请求账单服务、不依赖 Pi 私有模块。私有模块只允许用于测试对照。
- Cache 命中率是 assistant 请求的派生展示，不计后台预热；订阅额度来自 `subscription-usage/status/v1`，与会话账单独立。
- Fast 资格与开关归 Fast 扩展；界面只消费 `fast` 状态。自定义 segment 通过 `session-ui/statusline/register/v1` 注册，且须由配置显式选中。
- UI Meta 复用主模型正常响应，不额外调用模型；仅在交互式 TUI 注入协议。隐藏记录须在流式展示和持久化前清理，Recap 不进入模型上下文；过滤控制字符并限制长度，忽略无效、过期或失败响应。
- 手工命名锁只约束自动 session 名称，不阻止任务标题更新。协议字段和解析行为以实现及测试为准，不在文档维护第二份协议样例。
- 修改适配须同时检查生产者、转换、渲染和跨 session 生命周期。保留可测试、可替换的边界，实际公开发布时再评估泛化与兼容承诺。

### Fast：独立公共扩展

- 整个目录应可单独取出安装与测试，不依赖父仓库安装器、兄弟目录或个人配置。
- 通过 Pi 标准扩展 API 和运行时配置目录工作，不硬编码用户目录或账号。Codex 不按模型 ID 白名单过滤；Grok Fast 只包含已核实的 `xai/grok-4.7` → `grok-4.7-build-fast` 替换，不预置其他供应商映射。
- 开关按 provider 记忆，立即写入运行时状态文件（`state/fast.json`），写前重新读取并合并，只覆盖本次改动的 provider。扩展不回写安装器托管的配置文件；配置只提供还没设过开关时的默认值。没有 Fast 策略的 provider 拒绝切换且不写状态。
- 切换前先判断当前模型是否适用，不适用则拒绝切换并直接报失败；该 provider 已保存的"开启"在此时改写为关闭，避免状态声称开着却从不生效。配置无效不算陈旧状态，不改写已保存开关。
- 命令输出只有一行结论（`Fast: on`／`Fast: off`／`Fast: unavailable (原因)`），不常驻免责声明或历史记录；仅在切换没有落地时追加简短说明。
- 扩展缺省关闭；仓库个人配置可开启，但不得改变独立使用契约。配置、认证判断和请求策略归扩展自身负责。
- Codex 仅处理 OAuth 适用请求；保留已有 `service_tier`，不读取凭据文件，不新增网络请求、隐式重试、降级或费用估算。
- Grok 4.7 Fast 不在公开 xAI API 上。开关开启且当前模型为 `xai/grok-4.7` 的 OAuth 会话时，用 `setModel` 传入 provider 与 id 完全不变的副本，只覆盖 `baseUrl`、代理头和估价，并在 payload 里改模型名；目录外的模型 id 不得进入会话记录、模型范围或模型记忆，旧会话里残留的 fast id 要换回 `xai/grok-4.7`。
- 代理按 Grok CLI 的客户端标识和版本放行，因此代理请求声明客户端标识与版本，版本可配置以应对版本下限上移。不把 fast 模型名发到公开 API，不把 API key 送到代理。
- 区分允许尝试与后端实际接受，不根据速度、成功状态码或费用推测最终结果；`Fast: on` 只表示本地处理已生效。
- 通过标准 `ctx.ui.setStatus()` 的 `fast` 键发布 `fast` 或清除状态。自定义 footer 从 `footerData.getExtensionStatuses()` 读取；位置、颜色与布局归 UI 扩展，禁止反向依赖 session-ui。

## 4. 修改与文档规范

- 完成最小完整改动，不顺手重构无关模块；保留已有未提交修改。
- README 使用中文，按“是什么 → 快速开始 → 使用说明”组织。使用说明先区分通用规则和各 harness，再在所属 harness 下介绍插件，禁止把 harness 与插件并列；配置路径放到对应功能下，不用部署清单代替使用说明。
- 根 README 说明仓库个人默认值及简短的安装、更新和恢复操作，不记录迁移历史或内部回滚机制；独立扩展 README 不依赖仓库安装流程。
- 开发与设计规范只维护本文件，不再增加局部 AGENTS、设计过程、历史记录、测试结果或实现流水账文档；用代码和测试承载具体行为。
- 扩展代码注释、命令描述、运行提示和测试名称使用英文。
- 用户可见行为变化时同步更新对应 README 和针对性测试；升级 Pi 时重新核对扩展事件、认证及编辑器接口。

## 5. 验证规范

仓库没有项目级 `package.json` 或 `tsconfig.json`。使用当前 Pi 支持且能直接运行 TypeScript 测试的 Node，在仓库根目录执行：

```bash
# 安装器：临时目录与模拟 pi，不操作真实安装
node --test tests/*.test.mjs

# 扩展单元测试：不需要账号或网络
node --test harnesses/pi/builtins/session-ui/*.test.ts
node --test harnesses/pi/builtins/fast/*.test.ts

# 安装版 Pi 集成对照：路径按本机实际安装位置填写；未设置时跳过
PI_PACKAGE_ROOT=/absolute/path/to/pi-coding-agent \
  node --test harnesses/pi/builtins/session-ui/*.integration.test.mjs
```

Fast 也应支持在其目录独立运行 `node --test ./fast.test.ts`。直接从仓库加载 session-ui 时使用 `harnesses/pi/builtins/session-ui/index.ts`；如需使用仓库配置，通过 `PI_SESSION_UI_CONFIG` 指向 `harnesses/pi/plugin-configs/session-ui/config.json` 的绝对路径；安装后仍读取插件目录内的 `config.json`。

改动后按风险选择验证。图片 Overlay、真实 TUI 布局、动画与标题、并发工具、交互命令、自动／手工命名和跨 session 生命周期仍需真实终端验证；本地模拟不能证明模型遵循协议、后端支持 priority，或 Grok Build 代理接受 Pi 的 xAI OAuth。真实模型验证可能消耗额度，须单独授权；如实说明跳过和未验证项。
